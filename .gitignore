# local dev only
.claude/
# loose source images (originals kept locally, optimized copies live in assets/)
Untitled*.png
2ff16b1f-*.jpeg
*.b64
.DS_Store

# raw source assets dropped at repo root — kept local only; optimised copies live in /assets/images
# (real site images all live under assets/, so ignore every root-level image + screenshots)
/*.pdf
/*.png
/*.jpg
/*.jpeg
/*.JPG
/*.JPEG
Screenshot*.png
/*.textClipping
Screenshot*
